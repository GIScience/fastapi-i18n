[project]
name = "fastapi-i18n"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.109.2",
    "httpx>=0.23.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
